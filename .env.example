# LLM
ANTHROPIC_API_KEY=
ANTHROPIC_WORKSPACE_ID=         # only if the key is not workspace-scoped
WARDEN_LLM=anthropic            # anthropic | mock
WARDEN_MODEL=claude-opus-5
WARDEN_EFFORT=medium           # low | medium | high | xhigh | max

# Embeddings: default = Chroma ONNX MiniLM (downloads once), hash = offline fallback
WARDEN_EMBEDDINGS=default

# Storage: empty = SQLite at data/state/warden.db; or postgresql+psycopg://user:pw@host/warden
WARDEN_DATABASE_URL=
WARDEN_TENANT=default

# Dashboard auth: none (localhost only) | basic | proxy (OIDC via oauth2-proxy etc.)
WARDEN_AUTH=none
WARDEN_USERS=                   # basic: name:hash:role,...  hash from `warden hash-password`
WARDEN_TRUSTED_PROXIES=127.0.0.1/32
WARDEN_HEC_TOKEN=               # Splunk HEC receiver at POST /services/collector/event

# Paths
WARDEN_DATA_DIR=data
WARDEN_LOG_FILE=data/sample_logs/auth.jsonl

# Detection registry: comma list of detection ids, empty = run them all
WARDEN_DETECTIONS=

# Detection tuning
WARDEN_BF_THRESHOLD=10          # failed logins from one source IP in window
WARDEN_BF_WINDOW_SEC=300
WARDEN_SPRAY_MIN_USERS=5        # distinct accounts failing from one IP in the window
WARDEN_SPRAY_MAX_PER_USER=3     # above this many tries per account it is brute force
WARDEN_TRAVEL_MAX_KMH=900       # impossible travel: fastest plausible ground+air speed
WARDEN_TRAVEL_MIN_KM=500        # ignore short hops; country centroids are coarse
WARDEN_MFA_THRESHOLD=5          # denied/timed-out MFA pushes for one user in 15 min

# Guardrails
WARDEN_AUTO_ACTION_MIN_RISK=80  # risk >= this auto-executes allowlisted actions
WARDEN_RULE_THRESHOLDS=password_spray:70   # per-rule override
WARDEN_HUMAN_APPROVAL_ACTIONS=lock_user   # comma list, always need analyst approval
WARDEN_IP_SAFELIST=10.0.0.1,10.0.0.2
